<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Error - GlowAura Admin</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

  <style>
    body {
      background-color: #f6efe7;
      font-family: 'Poppins', sans-serif;
    }

    .error-wrapper {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }

    .error-box {
      background: #fff;
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
      max-width: 500px;
      width: 100%;
    }

    .error-icon {
      font-size: 60px;
      color: #b7c7a5;
      margin-bottom: 15px;
    }

    .brand {
      font-size: 1.8rem;
      font-weight: 700;
      color: #2b2b2b;
    }

    .btn-back {
      background-color: #d6b09c;
      border: none;
      color: #2b2b2b;
      border-radius: 8px;
      padding: 10px 18px;
      font-weight: 500;
    }

    .btn-back:hover {
      background-color: #c39b87;
    }
  </style>
</head>
<body>

  <div class="error-wrapper">
    <div class="error-box">
      
      <div class="brand mb-3">GlowAura Admin</div>

      <i class="bi bi-exclamation-triangle-fill error-icon"></i>

      <h2 class="fw-bold mb-2">Oops! Something went wrong</h2>
      <p class="text-muted mb-4">
        The page you're looking for doesn’t exist or an unexpected error occurred.
      </p>

      <a href="<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Error - GlowAura Admin</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

  <style>
    body {
      background-color: #f6efe7;
      font-family: 'Poppins', sans-serif;
    }

    .error-wrapper {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }

    .error-box {
      background: #fff;
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
      max-width: 500px;
      width: 100%;
    }

    .error-icon {
      font-size: 60px;
      color: #b7c7a5;
      margin-bottom: 15px;
    }

    .brand {
      font-size: 1.8rem;
      font-weight: 700;
      color: #2b2b2b;
    }

    .btn-back {
      background-color: #d6b09c;
      border: none;
      color: #2b2b2b;
      border-radius: 8px;
      padding: 10px 18px;
      font-weight: 500;
    }

    .btn-back:hover {
      background-color: #c39b87;
    }
  </style>
</head>
<body>

  <div class="error-wrapper">
    <div class="error-box">
      
      <div class="brand mb-3">GlowAura Admin</div>

      <i class="bi bi-exclamation-triangle-fill error-icon"></i>

      <h2 class="fw-bold mb-2">Oops! Something went wrong</h2>
      <p class="text-muted mb-4">
        The page you're looking for doesn’t exist or an unexpected error occurred.
      </p>

      <a href="/admin/dashboard" class="btn btn-back">
        <i class="bi bi-arrow-left"></i> Back to Dashboard
      </a>

    </div>
  </div>

</body>
</html>
 class="btn btn-back">
        <i class="bi bi-arrow-left"></i> Back to Dashboard
      </a>

    </div>
  </div>

</body>
</html>
